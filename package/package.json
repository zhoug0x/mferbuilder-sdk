{
  "name": "package",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./dist",
    "prepack": "yarn clean && tsup src/index.ts",
    "build": "yarn run prepack"
  },
  "devDependencies": {
    "ethers": "^5.7.2",
    "ts-node": "^10.9.1",
    "tsup": "^6.5.0",
    "typescript": "^4.9.3"
  },
  "peerDependencies": {
    "ethers": "^5"
  },
  "dependencies": {
    "@wagmi/core": "^0.7.5"
  }
}